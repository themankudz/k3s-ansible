---
# Skipped in check mode on a fresh install (the unit isn't really there yet).
- name: Restart otelcol-contrib
  ansible.builtin.systemd:
    name: otelcol-contrib
    state: restarted
    daemon_reload: true
  when: not (ansible_check_mode and (_otelcol_deb | default({})) is changed)
