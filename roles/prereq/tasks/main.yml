---
- name: Include live-node-safe baseline tasks
  ansible.builtin.include_tasks: baseline.yml

- name: Include runtime-config tasks (require a k3s restart to take effect)
  ansible.builtin.include_tasks: runtime-config.yml
