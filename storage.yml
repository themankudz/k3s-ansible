---
- name: Configure node storage (Longhorn disk + Unraid NFS mounts)
  hosts: k3s_cluster
  gather_facts: true
  become: true
  environment: "{{ proxy_env | default({}) }}"
  roles:
    - role: node_longhorn
    - role: node_nfs
